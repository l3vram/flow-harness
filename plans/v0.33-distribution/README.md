# v0.33 — Clean distribution + `flow-mcp doctor`

Milestone: make flow-harness **usable outside this console** by making it cleanly
installable on another machine/host and self-verifiable before use. Generated by
flow-dev-company on 2026-09-07, planned at commit `cb6ab98`.

This directory indexes the plans for this run only. The master roadmap is
[`../ROADMAP.md`](../ROADMAP.md); milestone history is [`../../STATUS.md`](../../STATUS.md).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | `flow-mcp doctor` — self-diagnostic subcommand | P1 | M | — | DONE (self-built via flow-run; 1 bug caught+fixed at Gate B) |
| 002  | Publish-ready package (self-contained bin, license, files) | P1 | S | — | DONE (direct maintenance) |
| 003  | Document install path + doctor | P2 | S | 001, 002 | DONE (direct maintenance) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Waves (dependency graph)

- **Wave 1 (parallel)**: 001 + 002 — disjoint files.
  - 001 touches `packages/mcp-server/*` + `scripts/bundle-mcp.mjs`.
  - 002 touches root `package.json` + `LICENSE`.
- **Wave 2**: 003 — docs, depends on both 001 (doctor behavior) and 002 (install command, version).

## Dependency notes

- 003 requires 001 because it documents the doctor's exact flags/checks, and 002 because it
  documents the final install command, license, and version `0.33.0`.
- 001 and 002 have no edge: their file sets are disjoint and neither reads the other's output at
  build time (001's bundle version-inject reads whatever `package.json` says, in any order).

## Decisions deferred to the human (Gate A / Gate B)

- **License**: Plan 002 defaults to **MIT**. Confirm before it lands.
- **`npm publish`**: explicitly NOT done by the fleet. The package becomes publish-ready;
  the actual publish is a maintainer step (credentials, deliberate) at/after Gate B.

## Out of scope for this milestone (candidates for later)

- HTTP/remote MCP transport (roadmap #17).
- A standalone end-user CLI for the autonomous run (`flow build "<objective>"`).
- Driving the real external Android app end to end.
